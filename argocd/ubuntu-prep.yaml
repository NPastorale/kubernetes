apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ubuntu-prep
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: ubuntu-prep
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/ubuntu-prep/overlays/prod
    repoURL: https://github.com/NPastorale/kubernetes
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
