apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: impostor
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: impostor
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/impostor/overlays/prod
    repoURL: https://github.com/NPastorale/kubernetes
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
