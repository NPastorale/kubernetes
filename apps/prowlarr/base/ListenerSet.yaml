apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: prowlarr
spec:
  parentRef:
    name: internal
    namespace: nginx-fabric
  listeners:
    - name: https
      port: 443
      protocol: HTTPS
      hostname: prowlarr.nahue.ar
      tls:
        mode: Terminate
        certificateRefs:
          - group: ""
            kind: Secret
            name: prowlarr
      allowedRoutes:
        namespaces:
          from: Same
