apiVersion: v1
kind: Service
metadata:
  name: wireguard-spain
spec:
  type: LoadBalancer
  selector:
    app: wireguard-spain
  ports:
    - name: web
      protocol: UDP
      port: 51821
      targetPort: 51821
