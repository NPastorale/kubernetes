apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: impostor-https
spec:
  hostnames:
    - impostor.nahue.ar
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: external
      namespace: nginx-fabric
      sectionName: impostor-https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: impostor
          port: 80
          weight: 1
