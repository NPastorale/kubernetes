apiVersion: v1
kind: PersistentVolume
metadata:
  name: plex-config-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  claimRef:
    name: plex-config-pvc
    namespace: plex
  nfs:
    server: 192.168.1.2
    path: "/share/k8s-volumes/persistent/plex/config"

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: plex-persistent-data-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Ti
  accessModes:
    - ReadWriteMany
  claimRef:
    name: plex-persistent-data-pvc
    namespace: plex
  nfs:
    server: 192.168.1.2
    path: "/share/k8s-volumes/persistent/plex/data"

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: plex-ephemeral-data-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 20Ti
  accessModes:
    - ReadWriteMany
  claimRef:
    name: plex-ephemeral-data-pvc
    namespace: plex
  nfs:
    server: 192.168.1.2
    path: "/share/k8s-volumes/ephemeral/plex/data"
