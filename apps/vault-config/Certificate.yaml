apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
spec:
  secretName: vault-tls
  commonName: vault.nahue.ar
  dnsNames:
    - vault.nahue.ar
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
