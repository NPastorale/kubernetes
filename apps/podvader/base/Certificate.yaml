apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: podvader
  namespace: nginx-fabric
spec:
  secretName: podvader
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - podvader.com
    - www.podvader.com
