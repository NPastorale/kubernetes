apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web
  namespace: nginx-fabric
spec:
  secretName: web
  issuerRef:
    name: letsencrypt-classic-prod
    kind: ClusterIssuer
  dnsNames:
    - nahue.com
    - www.nahue.com
