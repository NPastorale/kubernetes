apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bind-external-data-pvc
spec:
  storageClassName: manual
  volumeName: bind-external-data-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
