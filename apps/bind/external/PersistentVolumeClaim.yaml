apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bind-external-config-pvc
spec:
  storageClassName: manual
  volumeName: bind-external-config-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
