apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bind-external
  labels:
    app: bind-external
spec:
  serviceName: bind-external
  replicas: 1
  selector:
    matchLabels:
      app: bind-external
  template:
    metadata:
      labels:
        app: bind-external
    spec:
      volumes:
        - name: bind-external
          configMap:
            name: bind-external
        - name: data
          nfs:
            path: "/k8s-volumes/persistent/bind/external/data"
            server: 10.10.20.10
      containers:
        - name: bind-external
          image: ghcr.io/npastorale/bind:9.18.19-r1@sha256:90faf5b5a2b7535350f0a99eea93adf374538e025d78430607bef83e32b2049e
          resources:
            requests:
              memory: "56Mi"
              cpu: "100m"
            limits:
              memory: "8Gi"
              cpu: "4"
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "Europe/Madrid"
          ports:
            - containerPort: 53
          livenessProbe:
            tcpSocket:
              port: 53
            initialDelaySeconds: 3
            periodSeconds: 3
          readinessProbe:
            tcpSocket:
              port: 53
            initialDelaySeconds: 3
            periodSeconds: 3
          volumeMounts:
            - name: bind-external
              mountPath: /etc/bind/named.conf
              subPath: named.conf
            - name: data
              mountPath: /etc/bind/master
              readOnly: false
