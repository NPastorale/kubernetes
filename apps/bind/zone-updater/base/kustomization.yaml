resources:
  - CronJob.yaml
  - RoleBinding.yaml
  - Role.yaml
  - ServiceAccount.yaml
configMapGenerator:
  - name: bind-external-updater
    files:
      - named.conf
  - name: zone-update
    files:
      - main.bash
