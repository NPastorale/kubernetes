apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bind-internal-data-pvc
spec:
  storageClassName: manual
  volumeName: bind-internal-data-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
