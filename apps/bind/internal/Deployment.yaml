apiVersion: apps/v1
kind: Deployment
metadata:
  name: bind-internal
  labels:
    app: bind-internal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bind-internal
  template:
    metadata:
      labels:
        app: bind-internal
    spec:
      volumes:
        - name: bind-internal
          configMap:
            name: bind-internal
        - name: config
          persistentVolumeClaim:
            claimName: bind-internal-config-pvc
      containers:
        - name: bind-internal
          image: npastorale/bind9:9.16.29
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "Europe/Madrid"
          ports:
            - containerPort: 53
          livenessProbe:
            tcpSocket:
              port: 53
            initialDelaySeconds: 3
            periodSeconds: 3
          readinessProbe:
            tcpSocket:
              port: 53
            initialDelaySeconds: 3
            periodSeconds: 3
          volumeMounts:
            - name: bind-internal
              mountPath: /etc/bind/named.conf
              subPath: named.conf
            - name: config
              mountPath: /etc/bind/master
              readOnly: false
