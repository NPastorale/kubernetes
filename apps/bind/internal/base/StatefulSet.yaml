apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bind-internal
  labels:
    app: bind-internal
spec:
  serviceName: bind-internal
  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: bind-internal
  template:
    metadata:
      labels:
        app: bind-internal
    spec:
      volumes:
        - name: bind-internal
          configMap:
            name: bind-internal
        - name: ddns-key
          secret:
            secretName: ddns-key-internal
        - name: data
          nfs:
            path: "/mnt/NAS-T/k8s-volumes/persistent/bind/internal/data"
            server: 10.10.20.10
      containers:
        - name: bind-internal
          image: ghcr.io/npastorale/bind:9.20.23-r0@sha256:e2345e2f6fad0f01446ce3a5e1f517ee897ad4d41e6bcf41731c88a3b7687a49
          lifecycle:
            preStop:
              exec:
                command:
                  - "/bin/sh"
                  - "-c"
                  - |
                    rndc -s localhost -k /etc/bind/ddns-key.conf freeze && \
                    rndc -s localhost -k /etc/bind/ddns-key.conf sync -clean && \
                    rndc -s localhost -k /etc/bind/ddns-key.conf thaw
          resources:
            requests:
              memory: "56Mi"
              cpu: "100m"
            limits:
              memory: "8Gi"
              cpu: "4"
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "Europe/Madrid"
          ports:
            - containerPort: 53
          livenessProbe:
            tcpSocket:
              port: 53
            initialDelaySeconds: 3
            periodSeconds: 3
          readinessProbe:
            tcpSocket:
              port: 53
            initialDelaySeconds: 3
            periodSeconds: 3
          volumeMounts:
            - name: bind-internal
              mountPath: /etc/bind/named.conf
              subPath: named.conf
            - name: ddns-key
              mountPath: /etc/bind/ddns-key.conf
              subPath: ddns-key.conf
            - name: data
              mountPath: /etc/bind/master
              readOnly: false
