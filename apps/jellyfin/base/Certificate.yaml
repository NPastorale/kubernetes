apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyfin
spec:
  secretName: jellyfin-tls
  issuerRef:
    name: letsencrypt-short-lived-prod
    kind: ClusterIssuer
  dnsNames:
    - jellyfin.nahue.ar
