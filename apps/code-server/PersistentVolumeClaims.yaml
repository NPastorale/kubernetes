apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code-server-config-pvc
spec:
  storageClassName: manual
  volumeName: code-server-config-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
