apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lancache-pvc
spec:
  storageClassName: manual
  volumeName: lancache-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Ti
