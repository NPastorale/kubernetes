apiVersion: apps/v1
kind: Deployment
metadata:
  name: loggerbot
  labels:
    app: loggerbot
spec:
  selector:
    matchLabels:
      app: loggerbot
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: loggerbot
    spec:
      runtimeClassName: "nvidia"
      containers:
        - name: loggerbot
          image: ttl.sh/loggerbot:24h@sha256:0171a5a7de38757816f91dcf3b3b2680d93abfa206641cd2c9fef8a35aaa0722
          resources:
            limits:
              nvidia.com/gpu: 1
          imagePullPolicy: Always
          env:
            - name: TELEGRAM_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: telegram-api-key
                  key: telegram-api-key
            - name: MODEL_SIZE
              value: "turbo"
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"
