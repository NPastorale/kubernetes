apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-config-pvc
spec:
  storageClassName: manual
  volumeName: unifi-config-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
